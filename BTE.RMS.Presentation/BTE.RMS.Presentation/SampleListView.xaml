<?xml version="1.0" encoding="utf-8" ?>
<pr:ContentPageViewBase xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pr="clr-namespace:BTE.Presentation.Portable;assembly=BTE.Presentation.Portable"
             x:Class="BTE.RMS.Presentation.SampleListView">
  <ListView ItemsSource="{Binding OveralObjectives}" SelectedItem="{Binding Path=SelectedOveralObjective}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal"
                       Padding="5, 0">
            <Label Text="{Binding Title}"
                   HorizontalOptions="StartAndExpand"
                   VerticalOptions="Center" />
            <Label Text="{Binding Periority}"
                   HorizontalOptions="StartAndExpand"
                   VerticalOptions="Center" />
            <Button Text="ehsan" Clicked="Button_OnClicked"></Button>

          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>

</pr:ContentPageViewBase>