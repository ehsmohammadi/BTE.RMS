<pr:ViewBase x:Class="BTE.RMS.Presentation.WPF.Today.TodaySummaryPlanningView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:pr="clr-namespace:BTE.Presentation.UI.WPF;assembly=BTE.Presentation.UI.WPF"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      FlowDirection="RightToLeft"
      d:DesignHeight="477" d:DesignWidth="621" d:DataContext="{StaticResource TodaySummaryPlanningVm}">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<Border Grid.Row="0" BorderThickness="1" BorderBrush="#dcdcdc" Margin="10" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="227"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical">
                    <Label>تاریخ مورد نظر خود را انتخاب کنید</Label>
                    <DatePicker Height="25" SelectedDate="{Binding ToDayDate}"></DatePicker>
                </StackPanel>

                <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding ToDayDate}"/>
            </Grid>

        </Border>-->
        <Border Grid.Row="0" BorderThickness="1" BorderBrush="#dcdcdc" Margin="10" Padding="4">
            <StackPanel> 
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="227"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="تاریخ مورد نظر را انتخاب کنید"/>
                        <DatePicker Text="{Binding ToDayDate}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Text="{Binding ToDayDate}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <Label Content="سر رسیدهای مالی"/>
                            <DataGrid Height="160" CanUserSortColumns="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="سر رسید مالی" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                        <StackPanel Grid.Row="1">
                            <Label Content="برنامه ریزی اهداف"/>
                            <DataGrid Height="160" CanUserSortColumns="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="فعالیت" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Grid>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Label Content="یادداشت ها و قرارهای ملاقات"/>
                    <ComboBox/>
                    <ScrollViewer Height="320">

                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</pr:ViewBase>
